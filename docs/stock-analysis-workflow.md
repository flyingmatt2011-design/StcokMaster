# StockMaster 股票分析流程

本文描述 StockMaster 当前的实际股票分析链路。批量分析与单股分析复用同一条分析管线，区别仅在于批量任务增加了排队、并发控制和停止补充后续任务的逻辑。

```mermaid
flowchart TD
    A[用户选择单只或多只股票] --> B[提交分析任务]
    B --> C{任务队列调度}
    C -->|等待执行| C1[状态：等待中]
    C1 --> C
    C -->|取得执行槽位| D[状态：分析中<br/>通过 SSE 推送进度]
    C -->|用户要求停止| C2[不再启动后续任务<br/>已开始任务继续完成]

    D --> E{存在可复用的<br/>LLM 重试上下文?}
    E -->|是| Q[复用已准备的数据上下文]
    E -->|否| F[获取并保存历史日线]

    F --> G[识别盘前 / 盘中 / 盘后 / 非交易日]
    G --> H1[获取实时行情]
    G --> H2[获取筹码分布]
    G --> H3[读取最近大盘环境]
    G --> H4[读取持仓与成本]

    H1 --> I[合并基础市场上下文]
    H2 --> I
    H3 --> I
    H4 --> I

    I --> J1[财务、成长与盈利]
    I --> J2[资金流与龙虎榜]
    I --> J3[行业、概念与板块排名]
    I --> J4[当前估值与五年估值分位]
    I --> J5[机构、股东等基本面]
    I --> K[技术趋势分析]

    J1 --> L[聚合基本面上下文]
    J2 --> L
    J3 --> L
    J4 --> L
    J5 --> L

    K --> K1[均线 / 乖离率 / MACD / RSI]
    K1 --> K2[量价 / 趋势 / 支撑 / 压力]
    K2 --> K3[沿用原项目技术信号与评分]
    K2 --> K4[识别形态，仅作上下文<br/>不参与现有评分]

    L --> M1[上市公司公告]
    L --> M2[机构研报]
    L --> M3[互动问答]
    L --> M4[新闻搜索 API]
    L --> M5[本地历史资讯]

    M1 --> N[资讯过滤、去重与时效校验]
    M2 --> N
    M3 --> N
    M4 --> N
    M5 --> N

    K3 --> P[组装统一 Analysis Context]
    K4 --> P
    N --> P
    L --> P
    I --> P

    P --> P1[记录数据来源、时间与完整性]
    P1 --> P2[缓存已准备上下文<br/>供 LLM 失败后快速重试]
    P2 --> Q

    Q --> R{按设置的模型优先级调用 LLM}
    R -->|有效结构化 JSON| S[解析并校验分析结果]
    R -->|超时 / 空响应 / JSON 无效| R1{还有备用模型?}
    R1 -->|是| R
    R1 -->|否| X[任务失败<br/>保留诊断和重试上下文]

    S --> T[完整性检查与必要字段修复]
    T --> U[结合结构、市场阶段和风险<br/>校准明显矛盾的操作建议]
    U --> V[保存分析历史、资讯证据<br/>上下文快照和运行诊断]
    V --> W[生成结构化报告]
    W --> W1[评分 / 建议 / 趋势]
    W --> W2[核心结论 / 风险 / 催化]
    W --> W3[支撑 / 压力 / 止损 / 目标]
    W --> W4[新闻 / 数据质量 / 阶段耗时]
    W --> W5[报告底部 AI 追问]

    V --> Y[任务状态：完成]
    X --> Z[任务状态：失败]

    classDef user fill:#fff3d6,stroke:#c58b19,color:#3b2a08;
    classDef task fill:#e9efff,stroke:#5977c9,color:#14234d;
    classDef data fill:#e7f4ff,stroke:#3683b5,color:#12364f;
    classDef llm fill:#f2eaff,stroke:#8a5bc2,color:#32194f;
    classDef done fill:#e7f7ed,stroke:#3d9b60,color:#123b21;
    classDef fail fill:#ffe9e7,stroke:#bf4b43,color:#571b17;

    class A user;
    class B,C,C1,C2,D task;
    class F,G,H1,H2,H3,H4,I,J1,J2,J3,J4,J5,K,K1,K2,K3,K4,L,M1,M2,M3,M4,M5,N,P,P1,P2 data;
    class Q,R,R1,S,T,U llm;
    class V,W,W1,W2,W3,W4,W5,Y done;
    class X,Z fail;
```

## 关键规则

- 单股和批量分析共用同一套数据获取、技术指标、新闻分析、LLM 分析和评分逻辑。
- 基本面和资讯中的独立数据源尽可能并行获取；任务队列与 LLM 调用仍设置并发上限，避免触发限流。
- 单一数据源失败时按原有 fallback 链路降级；链路全部耗尽时记录缺失及原因，不把“无数据”解释成“无风险”。
- LLM 调用失败后优先复用已经准备好的数据上下文，只重新执行模型生成、结果校验和报告保存。
- 新增的估值分位、形态识别和市场温度只用于提供分析背景，不参与原有评分和买卖判断计算。
- 底层保留“买入 / 观望 / 卖出”结论；界面根据持仓状态映射为“加仓 / 持仓 / 减仓 / 清仓”或“建仓 / 观察 / 忽略”。

## 主要进度阶段

| 进度 | 阶段 | 主要工作 |
| ---: | --- | --- |
| 12% | 准备任务 | 初始化单股分析和重试上下文 |
| 16% | 行情就绪 | 历史日线获取并保存完成 |
| 18% | 实时与筹码 | 获取实时行情、筹码和市场阶段 |
| 32% | 基本面与趋势 | 聚合基本面并计算技术趋势 |
| 46% | 资讯检索 | 获取公告、研报、互动问答和新闻 |
| 58% | 上下文整理 | 汇总技术、基本面、资讯和持仓信息 |
| 64%–68% | 模型生成 | 调用主模型或按顺序切换备用模型 |
| 94% | 结果校验 | 解析 JSON、检查完整性和校准建议 |
| 97% | 保存结果 | 写入报告、上下文快照和诊断数据 |
| 100% | 分析完成 | 前端展示结构化报告 |

## 主要耗时位置

```mermaid
flowchart LR
    A[批量任务排队] --> B[第三方行情与基本面接口]
    B --> C[多维新闻搜索与失败重试]
    C --> D[LLM 生成]
    D --> E{响应是否有效?}
    E -->|否| F[JSON 修复或切换备用模型]
    F --> D
    E -->|是| G[解析、保存和展示]

    style C fill:#fff3d6,stroke:#c58b19
    style D fill:#f2eaff,stroke:#8a5bc2
    style F fill:#ffe9e7,stroke:#bf4b43
```

通常最不稳定、耗时波动最大的部分是新闻网络请求和 LLM 生成；日线技术指标计算本身通常较快。

## 性能与失败恢复护栏

- 模型链路只有一层显式 fallback：每个模型只进行一次传输调用，参数不兼容时仍允许一次去除不受支持参数的兼容重试；不会再叠加 LiteLLM Router 的隐式重试。
- 普通、Flash 和 Claude/Sonnet 高质量备用模型分别使用独立超时预算；同一模型连续失败后短期熔断，成功且响应稳定时逐步恢复允许的并发。
- 已准备输入会在调用 LLM 前写入本机检查点。桌面后台重启后，任务日志恢复的未完成股票可以直接重用相同输入，只重跑模型生成、结果校验和保存阶段。
- 运行流同时记录数据源尝试、每个模型尝试、结果校验耗时及评分轨迹。评分轨迹只解释“模型原始值 → 结构约束 → 市场阶段 → 大盘约束 → 最终动作”，不会再次改分。
- 首页大盘快照使用短时缓存和并发请求合并；批量个股分析期间暂停自动大盘刷新，避免与分析取数争抢免费源。

可在设置页调整以下稳定性参数：

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `LITELLM_ANALYSIS_TIMEOUT_SECONDS` | 90 | 普通分析模型单次预算 |
| `LITELLM_FAST_MODEL_TIMEOUT_SECONDS` | 60 | Flash 快速模型单次预算 |
| `LITELLM_QUALITY_FALLBACK_TIMEOUT_SECONDS` | 120 | Claude/Sonnet 最终备用单次预算 |
| `LITELLM_CIRCUIT_FAILURE_THRESHOLD` | 2 | 连续失败熔断阈值 |
| `LITELLM_CIRCUIT_COOLDOWN_SECONDS` | 180 | 熔断冷却时间 |
